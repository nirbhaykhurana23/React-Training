class q7{

    static addition(a, b) {

        return `${a} + ${b} = ${a + b}`;
    }

    static multiplication(a, b) {
        return `${a} * ${b} = ${a * b}`;
    }

    static subtraction(a, b) {
        return `${a} - ${b} = ${a - b}`;
    }

    static division(a, b) {
        return `${a} / ${b} = ${a / b}`;
    }
}

export { q7 };